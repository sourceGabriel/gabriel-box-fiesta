node_modules/
**/node_modules/
**/dist/
coverage/
*.log
.DS_Store
!/.idea/
!/.idea/workspace.xml
.idea/workspace.xml

# Claude Code: personal permission grants (per-machine); shared config is .claude/settings.json
.claude/settings.local.json

# runtime data (task-list helper, reverted feature)
server/data/

# owner portrait drop folder (sources live in tools/gabsinto-source/)
/gabriel-source/

# sound pack: only the curated/renamed output in ui/src/sound-assets/ is vendored
# (re-populate from the Kenney packs with: python tools/build-sound-pack.py --import <dir>)
/tools/sound-source/
