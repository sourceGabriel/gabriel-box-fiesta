import type { GamePersonality } from '../types';

/** Coup's catalog personality — palace intrigue, no conscience. */
export const personality: GamePersonality = {
  accent: '#e0b13c',
  vibe: 'TRAIÇÃO',
  blurb: 'Minta na cara dura. Quem hesita perde a cabeça.',
};
