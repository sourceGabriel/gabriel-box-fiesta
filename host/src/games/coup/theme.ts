import type { HostTheme } from '@party/ui';

/** Coup — a poker table under a single hanging lamp. Noir, tense, gold + blood. */
export const coupTheme: HostTheme = {
  accent: '#e0b13c',
  accentSecondary: '#c23b3b',
  background: 'noir',
  texture: 'grain',
  motion: 'tense',
};
