import coverUrl from './cover.webp';

/** Dilema nos Trilhos catalog cover — owner-supplied key art (see cover.CREDITS.md). */
export function DilemaCover() {
  return <img src={coverUrl} alt="Dilema nos Trilhos" className="game-cover" />;
}
