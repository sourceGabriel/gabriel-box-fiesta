import type { GamePersonality } from '../types';

/** Sabe-Tudo's catalog personality — smug, with a scoreboard. */
export const personality: GamePersonality = {
  accent: '#a78bfa',
  vibe: 'SOBERBA',
  blurb: 'Prove com números que você é o único inteligente da sala.',
};
