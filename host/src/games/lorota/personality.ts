import type { GamePersonality } from '../types';

/** Lorota!'s catalog personality — everyone lies, badly. */
export const personality: GamePersonality = {
  accent: '#2ee6a6',
  vibe: 'MENTIRA',
  blurb: 'Todo mundo mente. Só não seja o pior nisso.',
};
