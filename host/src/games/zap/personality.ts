import type { GamePersonality } from '../types';

/** Zap!'s catalog personality — your wit on trial. */
export const personality: GamePersonality = {
  accent: '#ff3caf',
  vibe: 'EGO',
  blurb: 'Suas melhores piadas julgadas por quem te conhece demais.',
};
