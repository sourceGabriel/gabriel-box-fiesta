import coverUrl from './cover.webp';

/** Fase 10 catalog cover — owner-supplied key art (see cover.CREDITS.md). */
export function Fase10Cover() {
  return <img src={coverUrl} alt="Fase 10" className="game-cover" />;
}
